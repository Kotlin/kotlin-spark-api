plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.3.71' apply false
}

allprojects {
    group = 'org.apache.spark'
    version = '0.1-SNAPSHOT'
}

subprojects {
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }
}
