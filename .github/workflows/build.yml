name: Build and test

on:
  push:
    branches: '*'
  pull_request:
    branches: '*'

jobs:
  build-scala-12:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          distributions: adopt
          java-version: 11
          check-latest: true

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      #      - name: Cache Gradle packages
      #        uses: actions/cache@v2
      #        with:
      #          path: |
      #            ~/.gradle/caches
      #            ~/.gradle/wrapper
      #          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
      #          restore-keys: |
      #            ${{ runner.os }}-gradle-

      - name: Make gradlew_all_versions executable
        run: chmod +x ./gradlew_all_versions_parallel
#
#      - name: Make gradlew executable
#        run: chmod +x ./gradlew

      - name: Build with Gradle
#        run: ./gradlew_all_versions_parallel build
        run: ./gradlew_all_versions clean build
#  qodana:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#      - name: 'Qodana Scan'
#        uses: JetBrains/qodana-action@v5.0.2

# vim: ts=2:sts=2:sw=2:expandtab
